# Commit Tool

Commit tool for git.

## Install

```bash
npm i @4wd/commit-tool -g
```

## Usage

* Commit changes:

```bash
commit [commit message]
```

* Create branch:

```bash
commit -b [branch name]
```
